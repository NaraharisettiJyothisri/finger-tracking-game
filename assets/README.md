# Pulse Runner - Assets Directory

This folder is created to satisfy standard directory structures. 

Note: All sound effects are generated dynamically at runtime using the **Web Audio API** (`SoundSynth` in `game.js`). This optimizes load times, eliminates external asset dependencies, and provides zero-latency retro sound effects.
